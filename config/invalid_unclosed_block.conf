# INVALID - Unclosed server block (missing closing brace)
server {
    listen 8080;
    server_name localhost;
    root ./www;
    
    location / {
        methods GET;
    }
# Missing closing brace for server block!
